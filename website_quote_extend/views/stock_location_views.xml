<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="view_location_description_form" model="ir.ui.view">
        <field name="name">stock.location.description.form</field>
        <field name="model">stock.location</field>
        <field name="inherit_id" ref="stock.view_location_form"/>
        <field name="arch" type="xml">
            <field name="comment" position="after">
                <field name="description" placeholder="What are  the activities in this location. e.g. Picking"/>
            </field>
        </field>
    </record>
</odoo>
